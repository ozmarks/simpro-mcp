name: Build container

# Builds the server image and pushes it to GitHub Container Registry (GHCR).
#
# Trigger:
#   - Manual run only (Actions tab -> "Run workflow"). No scheduled or push runs.
#
# The image is published to ghcr.io/<owner>/<repo>, tagged with the short commit
# SHA and `latest`.

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write # push to GHCR

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=sha,format=short
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
